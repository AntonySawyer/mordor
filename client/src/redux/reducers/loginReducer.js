let initialState = {
    isAuth: true,
    userId:'5df9dbf3a8b371205848e458',
    role: 'admin'
};

const loginReducer = (state = initialState, action) => {

    return state;
}

export default loginReducer;